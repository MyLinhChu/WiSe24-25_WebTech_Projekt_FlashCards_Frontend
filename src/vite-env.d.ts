/* Dieser Kommentar stellt sicher, dass TypeScript die Typen von Vite erkennt.
   Die `vite/client`-Typen definieren spezielle Vite-Funktionen wie dynamisches Laden von Modulen
   (`import.meta.env`). Ohne diese Zeile könnte TypeScript Fehler werfen, wenn Vite-spezifische
   Features verwendet werden. */
/// <reference types="vite/client" />
